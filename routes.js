//Global
const HOME = "/";
const JOIN = "/join";
const LOGIN = "/login";
const LOGOUT = "/logout";
const MYPAGE = "/mypage";

//Join
const JOIN_BUYER = "/join_buyer";
const JOIN_SELLER = "/join_seller";
const TERM_BUYER = "/term_buyer";
const TERM_SELLER = "/term_seller";

//User
const USERS = "/users";

const routes = {
  home: HOME,
  join: JOIN,
  join_buyer: JOIN_BUYER,
  join_seller: JOIN_SELLER,
  term_buyer: TERM_BUYER,
  term_seller: TERM_SELLER,
  login: LOGIN,
  logout: LOGOUT,
  users: USERS,
  mypage: MYPAGE
};
export default routes;
